spring.application.name=backend

# MySQL Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/devarakondi_silks?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool (Clever Cloud Limit is 5)
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Server Configuration
server.port=${PORT:8080}

# SQL Initialization
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# Mail Configuration (Supports Render Environment Variables)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${SPRING_MAIL_USERNAME:mahaan1699@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:fkdrujpzaowoawya}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
store.owner.email=${STORE_OWNER_EMAIL:devarakondisilks99@gmail.com}


# Admin Security
admin.secret=${ADMIN_SECRET:devarakondi_admin_123}

# Twilio WhatsApp Configuration (Supports Render Environment Variables)
twilio.account.sid=${TWILIO_ACCOUNT_SID:ACe2c0dd1f7d248eae38d2afb46c9576d8}
twilio.auth.token=${TWILIO_AUTH_TOKEN:c586dc9757eaf762756058d461b95340}
twilio.whatsapp.from=${TWILIO_WHATSAPP_FROM:+14155238886}
twilio.whatsapp.owner=${TWILIO_WHATSAPP_OWNER:+919972957102}

# Razorpay Configuration (Supports Render Environment Variables)
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SknLe92pLg24yi}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:az03cK6h1KbVmVhmmZ2LdByP}
